# NormScale 协作规则

> 本项目使用 Project Cairn 组织项目知识：`AGENTS.md` 是规则与导航入口，`cairn/` 是项目知识与状态层。
> 同目录下的 `CLAUDE.md` 仅包含单行 `@AGENTS.md`，以便 Claude Code 读取相同规则；Codex 直接读取本文件。

## 项目一句话简介

基于工业国家/行业执行标准的质量证明书合规检验引擎与智能比对系统

> 本文件由 `cairn init` 生成，已填充本项目自身定位与 provider 配置；其他项目在复用前应运行各自的初始化流程。

## 初始化配置

- Graduation provider(s): 暂缓对接（首次毕业时再连接知识库）
- Knowledge base index: 尚未配置
- Graduation target: 尚未配置

## 进入项目后的阅读顺序

1. 首先阅读本文件（AGENTS.md）。
2. 若 `cairn/ROADMAP.md` 存在，阅读路线图、当前焦点与开放问题（ROADMAP 为可选；最小化初始化的项目可能没有该文件）。
3. 阅读 `cairn/LOG.md` 最新记录（最新条目在顶部），了解近期进展与关键决策。
4. 根据当前任务需要，查阅相关的 `cairn/` 知识专题文档。

## 文档职责分工

| 文件 | 角色 | 维护方式 |
|---|---|---|
| `AGENTS.md` (根目录) | 规则与导航 | 极少变更，≤ 60 行 |
| `CLAUDE.md` (根目录) | 单行 `@AGENTS.md` 桩 | 一次性写入，不再修改 |
| `cairn/ROADMAP.md` | 路线图与进展 | 原地更新，保持精简 |
| `cairn/LOG.md` | 按时间倒序的日志 | 新条目添加在顶部（最新优先），单条 ≤ 20 行，仅包含摘要与指针 |
| `cairn/<topic>.md` | 知识专题文档（当前真相） | 原地更新；踩坑放入正文独立小节并通过 `contains` 标记；修订在 LOG 中添加指针 |
| `cairn/Reference/` | 外部原始输入 | 按需创建；仅追加 |
| `cairn/Cited.md` | 知识库引用列表 | 仅保留指针，严禁复制原文 |

> 其他内容仅在有明确信号触发时创建（需要记录决策、踩坑已解决、目标跨越单次会话）——不预先构建空文档。工程资产（代码或流程依赖的契约/配置/规范）不归本系统管理，保留在代码树中，不放入 `cairn/`。

## 冲突仲裁规则

- 优先级：**知识专题文档 > LOG 历史记录**；规则层面的冲突以本文件为准。
- 业务与设计结论以 `cairn/` 知识专题文档中的最新记录为准，而非旧的 LOG 条目。

## 知识库消费反射

- 在开展其可复用内核（任何产出或依赖的结论）够格毕业的工作前，先查阅本项目自身的 cairn/ 知识专题文档；当前尚未连接外部知识库（provider 暂缓对接——详见上方“初始化配置”），外部索引检查与 cairn/Cited.md 引用机制将在连接后激活。

## 文档协作规则

- 在进行修改前，判断用户期望的是“讨论/建议”还是“直接修改文档”；当用户表示“先看看 / 先评估”时，优先提供分析——不要直接重写正式文档。
- 纠正以往判断时，追加修正说明，严禁静默覆盖。
- 未经确认的判断不得作为既定事实写入。

## 知识沉淀规则

- 取得实质性进展后，在 `cairn/LOG.md` 顶部追加一条记录（摘要 + 指针）；最终结论沉淀至 `cairn/` 知识专题文档中。
- **完成态回复门禁**：在做出任何完成声明前——包括但不限于工作已完成、已实现、已定稿、已更新、已同步、已验证、测试通过；问题已修复或已解决；成果已就绪可用；表述工作已结束；以及语义等价的措辞——执行 `references/maintenance.md` 中的 Cairn 检查点；仅更新触发矩阵要求的记录，验证无误后再行回复。显式的只读 / 不编辑请求禁止写入 Cairn。
- 跨项目的可复用经验在连接知识库后通过毕业机制沉淀（provider 暂缓对接——详见上方“初始化配置”）。
